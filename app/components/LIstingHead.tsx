"use client"
import React from "react"
import {ListingHeadProps} from "../types"

const ListingHead = ({
  locationValue,
  title,
  imageSrc,
  id,
  currentUser,
}: ListingHeadProps) => {
  return <div></div>
}

export default ListingHead
