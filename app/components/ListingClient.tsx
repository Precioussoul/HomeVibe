"use client"

import {ListingClientProps} from "../types"

const ListingClient = ({
  listing,
  currentUser,
  reservation,
}: ListingClientProps) => {
  return <div>ListingClient</div>
}

export default ListingClient
