"use client"

import {ListingCardProps} from "@/app/types"

const ListingCard = ({
  data,
  reservations,
  onAction,
  disabled,
  actionLabel,
  actionId,
  currentUser,
}: ListingCardProps) => {
  return <div>ListingCard</div>
}

export default ListingCard
